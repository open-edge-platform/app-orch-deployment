---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: grafanaextensions.app.edge-orchestrator.intel.com
spec:
  group: app.edge-orchestrator.intel.com
  names:
    kind: GrafanaExtension
    listKind: GrafanaExtensionList
    plural: grafanaextensions
    shortNames:
    - grafanaext
    singular: grafanaextension
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Display name of Grafana extension
      jsonPath: .spec.displayName
      name: Display Name
      type: string
    - description: Artifact name
      jsonPath: .spec.artifactRef.name
      name: Artifact Name
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.display
      name: Status
      type: string
    - jsonPath: .status.message
      name: Message
      type: string
    - description: Description of Grafana extension
      jsonPath: .spec.artifactRef.description
      name: Description
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: GrafanaExtension is the Schema for the grafanaextensions API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GrafanaExtensionSpec defines the desired state of GrafanaExtension
            properties:
              artifactRef:
                description: ArtifactRef contains artifact information
                properties:
                  artifact:
                    description: Artifact has the artifact contents, e.g., Grafana
                      dashboard JSON model
                    type: string
                  description:
                    description: Description contains the detailed information of
                      Grafana extension
                    type: string
                  name:
                    description: Name of the artifact
                    type: string
                  publisher:
                    description: Publisher of the artifact
                    type: string
                required:
                - artifact
                - name
                - publisher
                type: object
              displayName:
                description: DisplayName is the display name of this Grafana extension
                type: string
              project:
                description: Project refers to the owner project of this Grafana extension
                type: string
            required:
            - artifactRef
            - displayName
            - project
            type: object
          status:
            description: GrafanaExtensionStatus defines the observed state of GrafanaExtension
            properties:
              conditions:
                description: Conditions is a list conditions that describe the state
                  of the Grafana extension CR
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              display:
                description: A string to display in the CLI
                type: string
              lastStatusUpdate:
                description: Time of last status update for the Grafana extension
                type: string
              message:
                description: An informative error message if State is Down
                type: string
              reconciledGeneration:
                description: The last generation that has been successfully reconciled
                format: int64
                type: integer
              state:
                description: The state of the Grafana extension (Running / Down /
                  Terminating)
                type: string
            required:
            - state
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
